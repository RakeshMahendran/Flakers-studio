[pytest]
# Use importlib so tests under tests/backend/* don't collide with the
# real top-level ``backend`` package during collection.
addopts = --import-mode=importlib
testpaths = tests
asyncio_mode = strict
pythonpath = .
